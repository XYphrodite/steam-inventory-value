<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>SteamInvValue.Web</RootNamespace>
    <InvariantGlobalization>false</InvariantGlobalization>
  </PropertyGroup>
  <ItemGroup>
    <!-- Страница зашивается в сборку, иначе single-file exe остаётся без wwwroot. -->
    <EmbeddedResource Include="wwwroot\index.html" LogicalName="index.html" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\SteamInvValue.Core\SteamInvValue.Core.csproj" />
  </ItemGroup>
</Project>
